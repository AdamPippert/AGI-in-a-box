---
# Secret templates - DO NOT commit actual secrets!
# Use external secret management (Vault, External Secrets, SOPS) in production
apiVersion: v1
kind: Secret
metadata:
  name: router-secrets
  namespace: agi-system
  labels:
    app: geometry-router
type: Opaque
stringData:
  # Placeholder - replace with actual secrets in production
  # Use kubectl create secret or external secret management
  ANTHROPIC_API_KEY: ""
---
apiVersion: v1
kind: Secret
metadata:
  name: crewai-secrets
  namespace: agi-system
  labels:
    app: crewai-orchestrator
type: Opaque
stringData:
  # API keys for external services
  OPENAI_API_KEY: ""
  SERPER_API_KEY: ""
  ANTHROPIC_API_KEY: ""
---
# Example of using External Secrets Operator (if installed)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: agi-secrets
#   namespace: agi-system
# spec:
#   refreshInterval: "1h"
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: crewai-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: OPENAI_API_KEY
#       remoteRef:
#         key: secret/data/agi
#         property: openai_api_key
#     - secretKey: ANTHROPIC_API_KEY
#       remoteRef:
#         key: secret/data/agi
#         property: anthropic_api_key
